**Inserting data into tables:**
psql -U postgres -d sem3_coursework -f 02-inserting.sql

**To check roles:**
- `SET ROLE <role>;` - set role
- `RESET ROLE;` - back previous role

**To check users:**
psql -U <user> -d sem3_coursework